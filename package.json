{
  "name": "food-fast-root",
  "version": "1.0.0",
  "description": "Root management for FoodFast project",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "dev": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
    "backend": "npm run server --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "admin": "npm run dev --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
